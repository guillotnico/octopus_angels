/// Design tokens — opacities.
abstract final class AppOpacity {
  /// Disabled UI elements (buttons, inputs, icons).
  static const double disabled = 0.45;

  /// Locked achievement badges (not yet earned).
  static const double badgeLocked = 0.55;
}

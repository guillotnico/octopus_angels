/// Design tokens — border radii (logical pixels).
///
/// Organic "pebble" radii (asymmetric 8-value shapes) live on the
/// `OrganicShape` primitive, not here.
abstract final class AppRadius {
  static const double chip = 22;
  static const double field = 22;
  static const double card = 24;
  static const double sheet = 38;
  static const double pill = 23;
}
